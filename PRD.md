# PRD — AB1 파일명 일괄 변환기

**문서 버전:** 1.0  
**작성일:** 2026-05-14  
**작성자:** EJ20179  
**연관 문서:** BRD.md

---

## 1. 제품 개요

### 1.1 한 줄 정의

생거 시퀀싱 결과 파일(.ab1)의 파일명에 엑손 접두어를 일괄 삽입하는 브라우저 기반 단일 파일 웹 도구.

### 1.2 핵심 흐름

```
① ITEM 규칙 설정 (최초 1회)
        ↓
② .ab1 파일 업로드 (드래그 앤 드롭)
        ↓
③ 변환 미리보기 확인
        ↓
④ ZIP 다운로드
```

### 1.3 기술 스택

| 항목 | 내용 |
|---|---|
| 배포 형태 | 단일 HTML 파일 (index.html) |
| 런타임 | 브라우저 (서버 없음) |
| 외부 라이브러리 | SheetJS xlsx@0.18.5 (Excel 파싱), JSZip@3.10.1 (ZIP 생성) — CDN |
| 데이터 저장 | localStorage (키: `ab1r3`) |

---

## 2. 사용자 스토리

| ID | 사용자 | 목표 | 이유 |
|---|---|---|---|
| US-01 | 담당 연구원 | 여러 .ab1 파일을 한 번에 변환하고 싶다 | 건마다 수작업으로 이름을 바꾸면 시간이 오래 걸리고 오타가 생긴다 |
| US-02 | 담당 연구원 | 기존 Excel 접수 파일에서 규칙을 가져오고 싶다 | 이미 정리된 데이터를 다시 입력하는 것은 비효율적이다 |
| US-03 | 신규 입사자 | 가이드만 보고 도구를 혼자 사용하고 싶다 | 담당자에게 매번 물어보기 어렵다 |
| US-04 | 담당 연구원 | LHON처럼 Product 없이 Primer만 있는 파일도 변환하고 싶다 | 유전자마다 파일명 구조가 다르다 |
| US-05 | 팀 전체 | 사내 보안 요건을 지키면서 사용하고 싶다 | 환자 샘플 번호가 외부로 나가면 안 된다 |

---

## 3. 기능 요구사항

### F-01. ITEM 규칙 관리

#### F-01-1. 규칙 목록 조회
- 등록된 모든 ITEM 규칙을 테이블 형식으로 표시한다.
- 각 행에 ITEM명, 삽입 위치, 매핑 수, 변환 예시, 수정/삭제 버튼을 표시한다.
- Primer 단독 모드 규칙에는 "Primer 단독" 배지를 표시하여 일반 규칙과 구분한다.
- BRCA1 기본 규칙은 localStorage가 비어 있을 때 자동으로 복원된다.

#### F-01-2. 규칙 추가/수정
- ITEM명, 매핑 모드, 삽입 위치(pos), 매핑 테이블을 입력하는 모달 창을 제공한다.
- ITEM명 중복 등록 시 에러 메시지를 표시한다.
- 수정 시 ITEM명은 읽기 전용으로 표시한다.
- 저장 시 localStorage에 즉시 반영하며, AB1 파일이 올라와 있으면 미리보기를 자동 갱신한다.

#### F-01-3. 규칙 삭제
- 삭제 전 확인 다이얼로그를 표시한다.
- 삭제 후 미리보기를 자동 갱신한다.

---

### F-02. 매핑 모드

두 가지 모드를 지원하며, 규칙별로 독립적으로 설정된다.

#### F-02-1. 일반 모드 (Product × Primer → Prefix)

- 대상: Product(M1, M2…) + Primer 조합으로 접두어가 결정되는 유전자 (예: BRCA1, NOTCH3)
- 파일명 구조: `ITEM_날짜_번호_Product_Primer.ab1`
- 변환 결과: `ITEM_날짜_번호_[접두어]_Product_Primer.ab1`
- 매핑 테이블 컬럼: Product / Primer / Prefix

#### F-02-2. Primer 단독 모드 (Primer → Prefix)

- 대상: Product 없이 Primer만으로 접두어가 결정되는 단일 앰플리콘 유전자 (예: LHON)
- 파일명 구조: `ITEM_날짜_번호_Primer.ab1`
- 변환 결과: `ITEM_날짜_번호_[접두어]_Primer.ab1`
- 매핑 테이블 컬럼: Primer / Prefix (Product 열 숨김)
- 모드 전환 시 삽입 위치 힌트 문구 및 미리보기가 즉시 업데이트된다.

---

### F-03. 매핑 테이블 편집

#### F-03-1. 직접 입력
- "+ 행 추가" 버튼으로 빈 행을 추가하고 직접 입력한다.
- Primer 셀은 드롭다운(5쌍 10종 + "직접 입력...") 방식으로 선택한다.
- 행 삭제 버튼(✕)을 제공한다.

#### F-03-2. Primer 드롭다운
지원 Primer 10종을 5쌍의 optgroup으로 구성하여 표시한다.

| 쌍 | Forward | Reverse |
|---|---|---|
| 1 | M13-F | M13-R |
| 2 | pMAL-F | pMAL-R |
| 3 | pQE-F | pQE-R |
| 4 | T7-F | SP6-R |
| 5 | T3-F | T7t-R |

목록에 없는 Primer는 "직접 입력..." 선택 시 텍스트 입력란이 표시된다.

#### F-03-3. 짝꿍 프라이머 자동완성
- 각 행의 짝꿍 열에 짝꿍 존재 여부를 표시한다.
  - ✔짝 (초록): 이미 짝꿍 행이 존재함
  - `+ SP6-R` 버튼 (주황): 클릭 시 해당 짝꿍 행을 바로 추가
  - `-` (회색): 5쌍에 없는 Primer
- "↔ 짝꿍 자동 완성" 버튼: 테이블 전체를 검사하여 빠진 짝꿍 행을 일괄 추가한다.
- Primer 단독 모드에서는 Product 조건 없이 Primer만으로 짝꿍을 판별한다.

---

### F-04. Excel 가져오기 마법사

#### F-04-1. 파일 선택 및 파싱
- `.xlsx`, `.xls` 형식을 지원한다.
- SheetJS로 파싱하며, 첫 번째 시트를 사용한다.

#### F-04-2. 열 매핑 마법사
- 파일 파싱 후 열 매핑 창이 자동으로 열린다.
- 처음 6행을 미리보기로 표시하며, 선택한 열은 색상으로 강조된다.
- 사용자가 지정하는 항목:

| 항목 | 설명 |
|---|---|
| 시작 행 | 1행부터(헤더 없음) 또는 2행부터(1행=헤더) |
| Product 인식 방식 | 샘플ID 열에서 추출 (예: `NOTCH3_날짜_번호_M1`) 또는 Product 직접 열 (예: `M1`) |
| 샘플ID/Product 열 | 해당 열 선택 |
| Primer 열 | 프라이머명이 있는 열 |
| Prefix 열 | 없으면 "없음" 선택 가능 (나중에 직접 입력) |

- 알려진 Primer값을 탐색하여 Primer 열을 자동 감지한다.
- 하단에 "유효 행: N개"를 실시간으로 표시한다.

#### F-04-3. 가져오기 실행
- 동일 Product + Primer 조합이 이미 있으면 Prefix만 업데이트한다.
- 새로운 조합은 행으로 추가된다.
- 기존 행은 삭제되지 않는다.

---

### F-05. AB1 파일 업로드

- 드래그 앤 드롭 및 클릭하여 선택 방식 모두 지원한다.
- 여러 파일 동시 선택이 가능하다.
- 파일 선택 즉시 변환 처리를 시작한다.

---

### F-06. 파일명 변환 처리

- 각 파일명에서 ITEM을 추출(대소문자 무관)하여 해당 규칙을 조회한다.
- 규칙에 따라 접두어를 산출하고 새 파일명을 생성한다.
- 변환 불가 시 이유를 함께 반환한다:

| 이유 | 조건 |
|---|---|
| 규칙 없음 | ITEM에 해당하는 규칙이 없음 |
| 파트 수 부족 | 파일명의 `_` 분리 파트 수가 pos보다 적음 |
| 매핑 없음 (X+Y) | 규칙 테이블에 해당 Product+Primer(또는 Primer) 조합이 없음 |

---

### F-07. 변환 미리보기

- 변환 결과를 테이블로 표시한다: 번호 / ITEM 태그 / 기존 파일명 / 새 파일명 / 상태
- 상태: ✓ 변환됨 (초록) / ✗ 불가 (빨강)
- 상단에 전체 / 변환 가능 / 변환 불가 통계 칩을 표시한다.
- 규칙이 없는 ITEM은 경고 박스에 별도 표시한다.

---

### F-08. ZIP 다운로드

- 변환 가능한 파일만 ZIP으로 묶어 다운로드한다.
- ZIP 내 파일명은 변환된 새 파일명으로 저장된다.
- ZIP 파일명: 첫 번째 파일명의 앞 3개 파트를 사용한다.  
  예) `NOTCH3_260507_7370170.zip`
- 압축 방식: STORE (비압축, 바이너리 파일 특성상 무손실 유지)

---

### F-09. 사용 가이드

- 헤더 우측 상단의 "사용 가이드" 버튼으로 전체 페이지 가이드를 연다.
- 가이드는 메인 도구와 독립된 전체 페이지로 표시된다 (SPA 방식 전환).
- 섹션 구성:

| 섹션 | 내용 |
|---|---|
| 개요 | 도구의 목적 및 처리 흐름 |
| 파일명 구조 | ITEM/날짜/샘플번호/Product/Primer 각 파트 설명 |
| ITEM 규칙 설정 | 단계별 규칙 추가 방법 (일반 모드) |
| 매일 사용하기 | 파일 업로드 → 미리보기 → 다운로드 흐름 |
| Excel 가져오기 | 열 매핑 마법사 사용법 |
| 짝꿍 프라이머 | 5쌍 목록 및 자동완성 사용법 |
| Primer 단독 모드 | 단일 Product 유전자 규칙 설정 방법 및 비교표 |
| 문제해결 FAQ | 자주 발생하는 오류와 해결 방법 |

---

## 4. 비기능 요구사항

### NF-01. 보안
- 모든 파일 처리는 브라우저 내에서만 이루어지며, 파일 내용이 외부로 전송되지 않는다.
- CDN 라이브러리(SheetJS, JSZip) 외 외부 네트워크 통신이 없다.

### NF-02. 성능
- 파일 100개 업로드 및 미리보기 생성: 2초 이내
- ZIP 생성: 파일 용량에 비례하나 UI가 응답 불가 상태로 멈추지 않아야 한다.

### NF-03. 호환성
- Chrome / Edge 최신 버전에서 정상 동작해야 한다.
- 화면 폭 1024px 이상을 기준 레이아웃으로 한다.

### NF-04. 지속성
- 규칙은 localStorage에 저장되어 브라우저 재시작 후에도 유지된다.
- localStorage 초기화 시 BRCA1 기본 규칙으로 자동 복원된다.

### NF-05. 접근성
- 신규 입사자가 가이드를 읽고 5분 내 첫 변환 작업을 완료할 수 있어야 한다.

---

## 5. 데이터 구조

### 5.1 규칙 스키마 (localStorage: `ab1r3`)

```json
[
  {
    "item": "BRCA1",
    "pos": 4,
    "mode": "product_primer",
    "table": [
      { "product": "M1", "primer": "SP6-R", "prefix": "ex1" },
      { "product": "M1", "primer": "T7-F",  "prefix": "ex1" }
    ]
  },
  {
    "item": "LHON",
    "pos": 4,
    "mode": "primer_only",
    "table": [
      { "product": "", "primer": "pMAL-R", "prefix": "ND4" },
      { "product": "", "primer": "pMAL-F", "prefix": "ND4" }
    ]
  }
]
```

| 필드 | 타입 | 설명 |
|---|---|---|
| `item` | string | 유전자명 (대문자) |
| `pos` | number | 삽입 기준 파트 위치 (1-based) |
| `mode` | string | `"product_primer"` (기본) 또는 `"primer_only"` |
| `table` | array | 매핑 행 목록 |
| `table[].product` | string | Product명 (primer_only 모드에서는 `""`) |
| `table[].primer` | string | Primer명 |
| `table[].prefix` | string | 삽입할 접두어 |

### 5.2 변환 로직

**일반 모드 (product_primer):**
```
입력: ITEM_날짜_번호_Product_Primer.ab1
           ↑ pos=4 → index 3
출력: ITEM_날짜_번호_[prefix]_Product_Primer.ab1
```

**Primer 단독 모드 (primer_only):**
```
입력: ITEM_날짜_번호_Primer.ab1
                       ↑ pos=4 → index 3
출력: ITEM_날짜_번호_[prefix]_Primer.ab1
```

---

## 6. UI 사양

### 6.1 색상 시스템

| 용도 | 값 |
|---|---|
| 키 컬러 (강조) | `#dc2626` |
| 키 컬러 (호버) | `#b91c1c` |
| 배경 | `#f3f4f6` |
| 카드/패널 배경 | `#ffffff` |
| 연한 빨강 배경 | `#fef2f2` |
| 연한 빨강 테두리 | `#fecaca` |
| 헤더 하단 선 | `3px solid #dc2626` |

### 6.2 화면 구성

```
┌─────────────────────────────────────────────────────┐
│ Header: AB1 파일명 일괄 변환기         [사용 가이드] │
├─────────────────────────────────────────────────────┤
│ ▼ ITEM 변환 규칙 관리                               │
│   ┌──────┬────────┬──────┬──────────┬───────┐       │
│   │ ITEM │ 삽입위치│매핑수│ 변환예시  │ 수정/삭│       │
│   └──────┴────────┴──────┴──────────┴───────┘       │
│   [+ ITEM 규칙 추가]                                 │
├─────────────────────────────────────────────────────┤
│ ① AB1 파일 업로드 (드래그 앤 드롭)                  │
├─────────────────────────────────────────────────────┤
│ ② 변환 미리보기                                     │
│   [전체 N] [✓ 변환 가능 N] [✗ 불가 N]              │
│   ┌──┬──────┬──────────┬──────────┬────┐            │
│   │# │ ITEM │ 기존 파일명│ 새 파일명 │상태│            │
│   └──┴──────┴──────────┴──────────┴────┘            │
│                            [↓ ZIP 다운로드]          │
└─────────────────────────────────────────────────────┘
```

### 6.3 모달 구성 (ITEM 규칙 추가/수정)

```
┌──────────────────────────────────┐
│ ITEM 규칙 추가              [✕] │
├──────────────────────────────────┤
│ ITEM 이름 *                      │
│ [                              ] │
│                                  │
│ 매핑 모드                        │
│ [Product × Primer → Prefix]      │
│ [Primer → Prefix (단독)    ]     │
│                                  │
│ 삽입 위치: [4] 번째              │
│                                  │
│ 매핑 테이블  [↑Excel] [+행추가]  │
│             [↔짝꿍 자동완성]     │
│ ┌──────┬────────┬────────┬──┬──┐ │
│ │Prod  │ Primer │ Prefix │짝│✕│ │
│ └──────┴────────┴────────┴──┴──┘ │
│                                  │
│ 변환 미리보기                    │
│ 기존: ITEM_P2_P3_P4_프라이머.ab1│
│ 새로: ITEM_P2_P3_[접두어]_P4... │
├──────────────────────────────────┤
│              [취소]    [저장]    │
└──────────────────────────────────┘
```

---

## 7. 릴리스 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1.0 | 2026-05-14 | 최초 릴리스. ITEM 규칙 관리, 일반/Primer 단독 모드, Excel 가져오기 마법사, 짝꿍 자동완성, ZIP 다운로드, 사용 가이드 포함 |

---

## 8. 미결 사항 / 향후 검토

| 항목 | 내용 |
|---|---|
| 규칙 백업/복원 | localStorage 초기화 대비 규칙을 JSON/Excel로 내보내고 가져오는 기능 |
| 규칙 공유 | 팀원 간 동일 규칙 파일을 공유하는 방법 (현재: 각자 설정 필요) |
| 파일명 패턴 검증 | 업로드 시 파일명이 예상 패턴과 다를 경우 사전 경고 |
